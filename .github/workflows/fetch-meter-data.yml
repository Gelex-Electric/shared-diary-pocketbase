name: Fetch Meter Data (hourly)

on:
  schedule:
    - cron: "0 * * * *"   # mỗi giờ, 24 lần/ngày (giờ UTC)
  workflow_dispatch:        # cho phép chạy thủ công để test / backfill
    inputs:
      fetch_hours:
        description: "Số giờ lùi lại để lấy dữ liệu (mặc định 6; tăng lên để vá dữ liệu thiếu)"
        required: false
        default: "6"

permissions:
  contents: write

concurrency:
  group: fetch-meter-data
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      TZ: Asia/Ho_Chi_Minh   # toàn bộ job chạy theo giờ VN (UTC+7)
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet requests

      - name: Fetch data from API
        env:
          API_USER: ${{ secrets.API_USER }}
          API_PASS: ${{ secrets.API_PASS }}
          PB_ADMIN_EMAIL: ${{ secrets.PB_ADMIN_EMAIL }}
          PB_ADMIN_PASS: ${{ secrets.PB_ADMIN_PASS }}
          FETCH_HOURS: ${{ inputs.fetch_hours || '6' }}
        run: python scripts/fetch_meter_data.py

      - name: Commit & push CSV
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/datametter.csv
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: update meter data $(date +'%Y-%m-%d %H:%M ICT')"
            git pull --rebase origin ${{ github.ref_name }}
            git push
          fi
